# 実機での動作確認

**ARKit はシミュレータで動かない。**展示の目玉である AR 体験は、実機でしか確認できない。

最終更新: 2026-08-30

## 0. まず読む — 展示当日に動かなくなるリスク

**無料の Apple ID で署名した場合、アプリは7日で失効する。**インストールから7日経つと起動しなくなる。

| アカウント | 有効期間 | 展示への影響 |
|---|---|---|
| 無料（Personal Team） | **7日** | 展示の直前に必ず入れ直す必要がある |
| 有料（Apple Developer Program・年99ドル） | 1年 | 問題なし |

**展示は10月中旬。**無料アカウントで進めるなら、当日または前日の入れ直しを段取りに組み込む必要がある。忘れると会場でアプリが起動しない。

現在の署名IDは `Apple Development: tsubasa.m1121@icloud.com (4PM8SZR33W)`。**これが無料か有料かを確認してほしい。**Xcode の Settings → Accounts で、チーム名が「(Personal Team)」なら無料。

## 1. 前提の確認

| 項目 | 現状 |
|---|---|
| Xcode | 26.6 |
| 署名ID | `Apple Development: tsubasa.m1121@icloud.com (4PM8SZR33W)` |
| 登録済みデバイス | iPhone 11（ポリゴン） |

### iPhone 11 で確認が必要なこと

| 項目 | 状況 |
|---|---|
| ARKit ワールドトラッキング（6DoF） | **対応している**（A13 チップ） |
| 画像トラッキング | 対応している |
| LiDAR | **非搭載。**ただし D3-a で LiDAR を必須にしていないため問題ない |
| **iOSバージョン** | **要確認。**`project.yml` の `deploymentTarget` を 18.0 にしているが、端末のOSがそれ未満なら下げる必要がある |

**端末の iOS バージョンを確認して教えてほしい**（設定 → 一般 → 情報 → システムバージョン）。18.0 未満なら `project.yml` を書き換える（N-1）。

## 2. 手順

### 2-1. プロジェクトを生成する

`.xcodeproj` は `project.yml` からの生成物なので、リポジトリには入っていない。

```bash
cd ios/PlavoApp
xcodegen generate
open PlavoApp.xcodeproj
```

`xcodegen` が無ければ `brew install xcodegen`。

### 2-2. 署名を設定する

Xcode で開いたら、

1. 左のファイル一覧から **PlavoApp**（青いアイコン）を選ぶ
2. **TARGETS → PlavoApp** を選ぶ
3. **Signing & Capabilities** タブを開く
4. **Automatically manage signing** にチェックを入れる
5. **Team** で自分の Apple ID を選ぶ

Bundle Identifier が `dev.plavo.PlavoApp` のままで衝突する場合は、末尾に何か足して一意にする。

### 2-3. 実機に入れる

1. iPhone を Mac に USB で繋ぐ
2. iPhone 側で「このコンピュータを信頼しますか」に**信頼**を選ぶ
3. Xcode 上部の実行先で **ポリゴン（iPhone 11）** を選ぶ
4. **⌘R** で実行

### 2-4. 初回だけ必要な操作

無料アカウントの場合、初回起動時に「信頼されていないデベロッパ」と出る。

iPhone の **設定 → 一般 → VPNとデバイス管理 → デベロッパApp** から、自分の Apple ID を**信頼**する。

### 2-5. カメラの許可

起動して**カメラ**タブを開くと許可を聞かれる。許可しないと AR が動かない。

## 3. 実機で確認すること

**ここが今回の最大の未検証領域。**優先度順に並べる。

### 優先度 高 — これが動かないと展示が成立しない

| # | 確認内容 | 見るところ |
|---|---|---|
| 1 | **植物を検出できるか** | 鉢植えにカメラを向けて、吹き出しが出るか。出るまでの秒数 |
| 2 | **吹き出しが空間に留まるか** | 端末を左右に振る。吹き出しが画面端から消え、戻すと同じ場所に現れるか |
| 3 | **吹き出しが飛ばないか** | 震える・明後日の方向へ飛ぶ・地面にめり込む、が起きないか |
| 4 | **奥行きが妥当か** | 植物の少し上に浮いて見えるか。壁にめり込んだり、手前で巨大化したりしないか |

### 優先度 中 — 体験の質に効く

| # | 確認内容 | 見るところ |
|---|---|---|
| 5 | フレームレート | 吹き出しの追従がカクつかないか。30fps を割っていないか |
| 6 | 検出の誤り | 植物以外（机、人、壁）に反応してしまわないか |
| 7 | 発熱 | 10分連続で使って、熱で処理が落ちないか |
| 8 | バッテリー | 10分でどれくらい減るか |

### 優先度 低 — 環境依存

| # | 確認内容 | 見るところ |
|---|---|---|
| 9 | 暗い場所での挙動 | 展示会場の照明は特殊なことが多い |
| 10 | 逆光での挙動 | 窓際など |

### まだ確認できないこと

**パネルの画像トラッキング（時系列の再生）は、印刷したパネルが無いと確認できない。**パネルができたら AR Resource Group「PanelImages」に登録する（§5）。

## 4. うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| `Signing for "PlavoApp" requires a development team` | §2-2 の Team が未設定 |
| `Unable to install` | iPhone のロックを解除して、信頼の確認に答える |
| 起動直後に落ちる | 「信頼されていないデベロッパ」。§2-4 |
| 起動して7日後に落ちる | 無料アカウントの失効。入れ直す（§0） |
| カメラが真っ暗 | カメラの許可を拒否している。設定 → plavo → カメラ |
| 「ARはこの環境では動きません」と出る | シミュレータで実行している。実行先を実機に変える |
| 吹き出しが出ない | 植物が検出されていない。明るい場所で、背景と分離しやすい構図にして試す |

## 5. パネル画像の登録（パネルができてから）

時系列パネルの識別には ARKit の画像トラッキングを使う。

1. 印刷したパネルを**正面から**撮影する
2. Xcode で **Assets.xcassets → 右クリック → New AR Resource Group** を作り、名前を `PanelImages` にする
3. 撮影した画像をドラッグして入れる
4. 各画像の **Name** を `content/dialogues/timeline.json` の `panels[].key` と一致させる

| 画像の Name | 対応するパネル |
|---|---|
| `sprout` | 6日目・発芽 |
| `trueLeaf-early` | 14日目・本葉が出た |
| `trueLeaf-thirsty` | 23日目・水切れ |
| `trueLeaf-recovered` | 24日目・水やりの翌日 |
| `bud` | 38日目・つぼみ |
| `bloom` | 45日目・開花 |
| `seedSet` | 62日目・結実 |
| `withered` | 78日目・枯死 |

各画像に **Physical Size**（実際の印刷サイズ）を必ず設定する。これが吹き出しの位置と大きさの計算に使われる。

**パネルはマット紙・非光沢で印刷する。**光沢があると照明を反射し、認識精度が落ちる。

## 6. 説明員用の隠し操作

来場者に数値を見せないため（原則2）、モック操作は隠してある。

| 操作 | 場所 | 内容 |
|---|---|---|
| 長押し1.5秒 | カメラタブの下部 | 水やりボタン・水分スライダー・リセット |
| リセット | マイページタブ | 次の来場者のために記録を消す |

起動引数でタブを指定することもできる。

```bash
xcrun devicectl device process launch --device <name> dev.plavo.PlavoApp -startTab 2
```
