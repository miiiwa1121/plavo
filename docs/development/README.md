# development — 開発ドキュメントの目次

セットアップと開発の進め方。

| ファイル | 内容 | 状態 |
|---|---|---|
| [setup.md](./setup.md) | 環境構築と実行手順 | 初版 |
| ios-app.md | iOSアプリのセットアップ | 未作成（実装開始後） |

## 現在の実装状況

| 領域 | 状態 |
|---|---|
| 診断クライアント / 検証ハーネス（TypeScript） | **動作する。**D34により当日は使わず、セリフ制作の道具になる |
| センサーのサンプルデータ | **生成済み。**4シナリオ |
| 導出指標の計算 | **検証済み。**34件のチェックが通っている |
| iOSアプリ ドメイン層（`ios/PlavoCore/`） | **動作する。**テスト19件成功。実機不要で検証できる |
| iOSアプリ本体（`ios/PlavoApp/`） | 未着手。xcodegen で生成する方針 |
| セリフのプール（`content/`） | **114本。**違反1件・警告7件がレビュー待ち |
| ガジェット（ハードウェア） | 未着手 |

## 次にやること

**`PlavoApp` の作成。**ドメイン層（`ios/PlavoCore/`）は動いているので、次はアプリ本体。xcodegen でプロジェクトを生成し、タブ構成とセンサーのモックUIまで作ればシミュレータで動く。

**AR部分は実機が必要。**ARKit はシミュレータで動かない。

`server/` の役割が変わったことに注意。当日の実行環境ではなく、**セリフ制作の道具**である。
