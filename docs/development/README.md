# development — 開発ドキュメントの目次

セットアップと開発の進め方。

| ファイル | 内容 | 状態 |
|---|---|---|
| [setup.md](./setup.md) | 環境構築と実行手順 | 初版 |
| [device-setup.md](./device-setup.md) | **実機での動作確認**。ARはここでしか検証できない | 初版 |

## 現在の実装状況

| 領域 | 状態 |
|---|---|
| 診断クライアント / 検証ハーネス（TypeScript） | **動作する。**D34により当日は使わず、セリフ制作の道具になる |
| センサーのサンプルデータ | **生成済み。**4シナリオ |
| 導出指標の計算 | **検証済み。**34件のチェックが通っている |
| iOSアプリ ドメイン層（`ios/PlavoCore/`） | **動作する。**テスト19件成功。実機不要で検証できる |
| iOSアプリ本体（`ios/PlavoApp/`） | タブ構成が動作。**AR部分は実機での検証が未了** |
| セリフのプール（`content/`） | **114本。**違反1件・警告7件がレビュー待ち |
| ガジェット（ハードウェア） | 未着手 |

## 次にやること

**実機での AR 確認。**アプリの骨格はできてシミュレータで動いているが、**AR部分は一度も動作検証されていない。**手順は [device-setup.md](./device-setup.md)。

確認すべきことを優先度順にまとめてある。特に「吹き出しが空間に留まるか」「飛ばないか」は、動かないと展示が成立しない。

`server/` の役割が変わったことに注意。当日の実行環境ではなく、**セリフ制作の道具**である。
